---
title: "Hello World — Welcome to Pieron Labs"
date: "2026-02-09"
description: "Introducing the Pieron Labs blog. We build with Next.js, Convex, and AI."
tags: ["announcement", "nextjs", "ai"]
author: "Jack Pieron"
---

# Welcome to Pieron Labs

I'm excited to launch this technical blog where I'll share insights, tutorials, and lessons learned from building products with modern web technologies.

## What We Build

At Pieron Labs, we focus on three core technologies:

- **Next.js** for blazing-fast web applications
- **Convex** for real-time backends without the complexity
- **AI tools** to supercharge development workflows

## Why This Blog?

The indie developer community has given me so much. This blog is my way of giving back — sharing practical knowledge that helps you ship faster and build better.

## What to Expect

I'll be writing about:

- Next.js 15 patterns and best practices
- Building real-time apps with Convex
- Integrating AI into your workflow
- Startup lessons and product development
- Code snippets and mini-tutorials

## Example: A Simple Next.js Server Action

Here's a taste of what you'll find here — a basic server action in Next.js 15:

```typescript
'use server'

import { revalidatePath } from 'next/cache'

export async function createPost(formData: FormData) {
  const title = formData.get('title') as string
  const content = formData.get('content') as string

  // Your database logic here
  await db.posts.create({
    title,
    content,
    publishedAt: new Date(),
  })

  revalidatePath('/blog')
  return { success: true }
}
```

Simple, type-safe, and powerful. That's the Next.js way.

## TypeScript Types for Better DX

Here's how we define a blog post type:

```typescript
interface BlogPost {
  slug: string
  title: string
  description: string
  date: string
  tags: string[]
  author: string
  content: string
  readingTime: number
}

// Utility function to parse frontmatter
function parsePost(fileContent: string): BlogPost {
  const { data, content } = matter(fileContent)
  return {
    ...data,
    content,
    readingTime: calculateReadingTime(content),
  } as BlogPost
}
```

## Let's Build Together

I'm building in public and sharing everything I learn. Follow along, ask questions, and let's push the boundaries of what's possible with modern web development.

**— Jack Pieron**  
Founder, Pieron Labs  
Grand Rapids, MI
